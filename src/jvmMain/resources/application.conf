llm {
    # Which LLM provider to use: "openrouter" or "openai"
    provider = "openrouter"

    openrouter {
        # Your OpenRouter API key (already configured!)
        apiKey = "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"
        apiKey = ${?OPENROUTER_API_KEY}  # Override with env var if set

        baseUrl = "https://openrouter.ai/api/v1"
        model = "openai/gpt-oss-20b:free"  # Cheap and fast!
        # Other models: "openai/gpt-4", "anthropic/claude-3-sonnet"
        maxTokens = 2000
    }

    openai {
        # Only if you have OpenAI API key (not needed with OpenRouter!)
        apiKey = ""
        apiKey = ${?OPENAI_API_KEY}  # Override with env var if set

        model = "gpt-4"
        maxTokens = 2000
    }
}

database {
    sqlite {
        path = "./data"
    }
}

api {
    openalex {
        # Your email for polite API usage
        email = "bekhouche.mouadh@univ-oeb.dz"
        email = ${?OPENALEX_EMAIL}  # Override with env var if set
    }
}

